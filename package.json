{
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19",
    "@commitlint/config-conventional": "^19",
    "husky": "^9",
    "lint-staged": "^15"
  },
  "lint-staged": {
    "*.rs": [
      "cargo fmt -- --check"
    ]
  }
}
